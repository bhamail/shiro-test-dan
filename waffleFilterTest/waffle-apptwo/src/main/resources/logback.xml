<configuration>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${catalina.base}/logs/waffle-apptwo.log</file>

        <encoder>
            <pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="FILE" />
        <appender-ref ref="STDOUT" />
    </root>


    <logger name="waffle.servlet.NegotiateSecurityFilter" level = "FINE"/>
    <logger name="waffle.servlet.spi.SecurityFilterProviderCollection" level = "FINE"/>
    <logger name="waffle.servlet.spi.NegotiateSecurityFilterProvider" level = "FINE"/>
    <logger name="waffle.servlet.spi.BasicSecurityFilterProvider" level = "FINE"/>
</configuration>